const left = document.querySelector(".split-left")
const right = document.querySelector(".split-right")
const container = document.querySelector(".main-container")

left.addEventListener('mouseenter', () => main-container.classList.add('hover-left'))
left.addEventListener('mouseleave', () => main-container.classList.remove('hover-left'))

right.addEventListener('mouseenter', () => main-container.classList.add('hover-right'))
right.addEventListener('mouseleave', () => main-container.classList.remove('hover-right'))
